#include "main.h"
/**
 * print_alphabet - print alphabets in lowercase
 * Return: Always 0 (success)
 */
int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
