#include "main.h"
/**
 * _abs - compute the absolute value
 * @n: number to be computed
 * Return: Always 0 (success)
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	return (0);
}
